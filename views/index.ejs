<!DOCTYPE html>
<html>
<head>
  <title>My Blog</title>
  <link rel="stylesheet" type="text/css" href="css/styles.css">
  <!-- Link to Bootstrap CSS (CDN) -->
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="/">My Blog</a>
  </nav>
  <div class="container my-5">
    <h1 class="mb-4">Welcome to My Blog</h1>
    <form action="/create" method="post">
      <div class="form-group">
        <input type="text" class="form-control" name="title" placeholder="Enter your blog title">
      </div>
      <div class="form-group">
        <textarea class="form-control" name="content" placeholder="Write your blog content"></textarea>
      </div>
      <button type="submit" class="btn btn-primary">Create Blog</button>
    </form>
    <h2 class="mt-5">Recent Blog Posts</h2>
    <ul class="list-group">
      <% for (let post of posts) { %>
        <li class="list-group-item">
          <h3><%= post.title %></h3>
          <p><%= post.content %></p>
          <p class="text-muted">Posted on <%= post.date.toLocaleDateString() %></p>
        </li>
      <% } %>
    </ul>
  </div>
</body>
</html>
