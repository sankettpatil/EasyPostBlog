]<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= post.title %> | EasyPostBlog</title>
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700&display=swap" rel="stylesheet">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">


  <style>
    body {
      background-color: #343a40;
      font-family: 'Roboto', sans-serif;
      color: #ffffff;
      padding: 40px 0;
    }
    .container {
      max-width: 800px;
      margin: 0 auto;
    }
    .navbar {
      background-color: #343a40;
      justify-content: center;
    }
    .btn-primary {
      background-color: #007bff;
      border: none;
      border-radius: 5px;
      padding: 10px 20px;
      font-size: 16px;
    }
    .post {
      background-color: #495057;
      border-radius: 5px;
      padding: 15px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      margin-bottom: 20px;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container">
      <a class="navbar-brand mx-auto" href="/">EasyPostBlog</a>
    </div>
  </nav>
  <div class="container">
    <div class="post">
      <h2 class="mb-4"><%= post.title %></h2>
      <p><%= post.body %></p>
      <!-- ... (previous content) -->
<p><small>Posted on <%= new Date(post.date).toLocaleDateString() %> at <%= new Date(post.date).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' }) %></small></p>
<!-- ... (remaining content) -->

    </div>
    <button class="btn btn-primary" onclick="window.location.href='/dashboard'">Back to Dashboard</button>
  </div>
</body>
</html>
